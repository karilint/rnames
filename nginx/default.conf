server {
    listen 80 default_server;
    server_name _;
    
    location /.well-known/acme-challenge/ {
      root /var/www/certbot;
    }

    location / {
      return 301 https://$host$request_uri;
    }
}

server {
    listen                    443 ssl;
    server_name               ${RNAMES_DOMAIN};
    ssl_certificate           /etc/nginx/ssl/live/${RNAMES_DOMAIN}/fullchain.pem;
    ssl_certificate_key	      /etc/nginx/ssl/live/${RNAMES_DOMAIN}/privkey.pem;
    ssl_protocols             TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_ciphers               EECDH+AESGCM:EDH+AESGCM;
    ssl_dhparam               /certs/dhparam.pem;
    ssl_ecdh_curve            secp384r1; 
    ssl_session_timeout       10m;
    ssl_session_cache         shared:SSL:10m;
    ssl_session_tickets       off;
    ssl_stapling              on;
    ssl_stapling_verify       on;

    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload";
    add_header X-Frame-Options DENY;
    add_header X-Content-Type-Options nosniff;
    add_header X-XSS-Protection "1; mode=block";

    resolver 127.0.0.11 valid=10s ipv6=off;
    resolver_timeout 5s;
    set $upstream_app http://web:8000;

    client_body_timeout 300;
    client_header_timeout 300;
    proxy_read_timeout 300;
    proxy_connect_timeout 300;
    proxy_send_timeout 300;

    # Send rate-limited clients to 429 page
    limit_req_status 429;

    # Rate-limited: signup and password-reset-request
    location /accounts/signup/ {
        limit_req zone=accounts_limit burst=3 nodelay;
        proxy_pass $upstream_app;
        proxy_set_header  Host               $host;
        proxy_set_header  X-Real-IP          $remote_addr;
        proxy_set_header  X-Forwarded-For    $proxy_add_x_forwarded_for;
        proxy_set_header  X-Forwarded-Proto  https;
    }

    location /accounts/password/reset/ {
        limit_req zone=accounts_limit burst=3 nodelay;
        proxy_pass $upstream_app;
        proxy_set_header  Host               $host;
        proxy_set_header  X-Real-IP          $remote_addr;
        proxy_set_header  X-Forwarded-For    $proxy_add_x_forwarded_for;
        proxy_set_header  X-Forwarded-Proto  https;
    }

    # Rate-limited: login
    location /accounts/login/ {
        limit_req zone=login_limit burst=10 nodelay;
        proxy_pass $upstream_app;
        proxy_set_header  Host               $host;
        proxy_set_header  X-Real-IP          $remote_addr;
        proxy_set_header  X-Forwarded-For    $proxy_add_x_forwarded_for;
        proxy_set_header  X-Forwarded-Proto  https;
    }

    location / {
        proxy_pass $upstream_app;
        proxy_set_header  Host               $host;
        proxy_set_header  X-Real-IP          $remote_addr;
        proxy_set_header  X-Forwarded-For    $proxy_add_x_forwarded_for;
        proxy_set_header  X-Forwarded-Proto  https;
    }

    location /static/ {
        alias /vol/web/static/;
    }
}

server {
    listen                    443 ssl;
    server_name               ${PMA_DOMAIN};
    ssl_certificate           /etc/nginx/ssl/live/${RNAMES_DOMAIN}/fullchain.pem;
    ssl_certificate_key	      /etc/nginx/ssl/live/${RNAMES_DOMAIN}/privkey.pem;
    ssl_protocols             TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_ciphers               EECDH+AESGCM:EDH+AESGCM;
    ssl_dhparam               /certs/dhparam.pem;
    ssl_ecdh_curve            secp384r1; 
    ssl_session_timeout       10m;
    ssl_session_cache         shared:SSL:10m;
    ssl_session_tickets       off;
    ssl_stapling              on;
    ssl_stapling_verify       on;

    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload";
    add_header X-Frame-Options DENY;
    add_header X-Content-Type-Options nosniff;
    add_header X-XSS-Protection "1; mode=block";

    resolver 127.0.0.11 valid=10s ipv6=off;
    resolver_timeout 5s;

    client_max_body_size  10M;
    client_body_timeout 300;
    client_header_timeout 300;
    proxy_read_timeout 300;
    proxy_connect_timeout 300;
    proxy_send_timeout 300;

    location / {
        set $upstream_pma http://phpmyadmin:80;
        proxy_pass $upstream_pma;
        proxy_set_header  Host               $host;
        proxy_set_header  X-Real-IP          $remote_addr;
        proxy_set_header  X-Forwarded-For    $proxy_add_x_forwarded_for;
        proxy_set_header  X-Forwarded-Proto  https;
    }
}
